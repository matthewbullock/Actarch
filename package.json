{
  "name": "actearch",
  "version": "1.0.0",
  "engines": {
    "node": "^10.11.0"
  },
  "scripts": {
    "start": "node backend/app.js",
    "build": "concurrently \"cd client && yarn start\" \"cd server && yarn start\"",
    "heroku-postbuild": "yarn build",
    "dev": "concurrently \"cd client && yarn start\" \"cd server && yarn start\""
  },
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "mongoose": "^5.3.4",
    "concurrently": "^3.5.0"
  }
}
